<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:FoodNet.Presentation.ViewModels"
             x:Class="FoodNet.Presentation.Views.RestaurantDetailPage"
             Title="Restaurant"
             BackgroundColor="{StaticResource Background}">

    <ScrollView>
        <StackLayout Padding="20" Spacing="20">
            <!-- Header / Basic Info -->
            <Frame BackgroundColor="{StaticResource CardBackground}"
                   BorderColor="{StaticResource Border}"
                   CornerRadius="12"
                   Padding="16"
                   HasShadow="True">
                <StackLayout Spacing="8">
                    <Label x:Name="RestaurantEmoji" Text="🍽️" FontSize="36" HorizontalOptions="Center"/>
                    <Label x:Name="RestaurantName" Text="Restaurant Name" FontSize="22" FontAttributes="Bold" HorizontalOptions="Center" TextColor="{StaticResource TextPrimary}"/>
                    <Label x:Name="RestaurantMeta" Text="⭐ 4.8 • 25-35 min" FontSize="14" HorizontalOptions="Center" TextColor="{StaticResource TextSecondary}"/>
                </StackLayout>
            </Frame>

            <!-- Offers & Voucher -->
            <StackLayout Spacing="12">
                <Label Text="Offers &amp; Vouchers" FontSize="20" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}"/>
                <CollectionView x:Name="OffersList" SelectionMode="None">
                    <CollectionView.ItemsLayout>
                        <LinearItemsLayout Orientation="Horizontal" ItemSpacing="12" />
                    </CollectionView.ItemsLayout>
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Frame BackgroundColor="{StaticResource Primary}"
                                   BorderColor="{StaticResource Primary}"
                                   CornerRadius="12"
                                   Padding="14,10"
                                   HasShadow="True">
                                <Label Text="{Binding .}" TextColor="White" FontAttributes="Bold" />
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

                <Frame BackgroundColor="{StaticResource Tertiary}"
                       BorderColor="{StaticResource Tertiary}"
                       CornerRadius="12"
                       Padding="14,10"
                       HasShadow="True">
                    <Label x:Name="VoucherText" Text="Voucher" TextColor="White" FontAttributes="Bold"/>
                </Frame>
            </StackLayout>

            <!-- Foods -->
            <StackLayout Spacing="12">
                <Label Text="Menu" FontSize="20" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}"/>
                <CollectionView x:Name="MenuList" SelectionMode="None">
                    <CollectionView.ItemsLayout>
                        <GridItemsLayout Orientation="Vertical" Span="3" />
                    </CollectionView.ItemsLayout>
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Frame BackgroundColor="{StaticResource CardBackground}"
                                   BorderColor="{StaticResource Border}"
                                   CornerRadius="12"
                                   Padding="12"
                                   HasShadow="True">
                                <StackLayout Spacing="8" HorizontalOptions="Center">
                                    <Frame BackgroundColor="{StaticResource Gray100}" BorderColor="{StaticResource Border}" CornerRadius="8" Padding="0" WidthRequest="80" HeightRequest="80" HorizontalOptions="Center">
                                        <Label Text="{Binding Emoji}" FontSize="28" HorizontalOptions="Center" VerticalOptions="Center"/>
                                    </Frame>
                                    <Label Text="{Binding Name}" FontSize="16" FontAttributes="Bold" TextColor="{StaticResource TextPrimary}" HorizontalOptions="Center"/>
                                    <Label Text="{Binding Price, StringFormat='${0:F2}'}" FontSize="14" TextColor="{StaticResource Primary}" HorizontalOptions="Center"/>
                                    <Button Text="Add"
                                            Background="{StaticResource Primary}"
                                            TextColor="White"
                                            CornerRadius="8"
                                            Margin="0"
                                            FontSize="14"
                                            Clicked="OnAddToCartClicked"/>
                                </StackLayout>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </StackLayout>
        </StackLayout>
    </ScrollView>

</ContentPage>

